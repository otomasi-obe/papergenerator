============================================================
  MASTER PROMPT PERBAIKAN AEJ_EMBEDDED_REALGEN.PY UNTUK TEMPLATE AEJ_EMBEDDED_REAL
============================================================
Template       : AEJ.docx
Output         : AEJ_embedded_real.docx
Generator      : AEJ_embedded_realgen.py
Total anomali  : 6
Kategori error : ERROR_STYLE, ERROR_TABLE
Catatan        : Skor utama BISA tetap tinggi -- sebagian temuan
                 di sini adalah anomali heuristik visual yang
                 tidak merusak struktur XML.

============================================================
  RINCIAN TEMUAN
============================================================

[STYLE] 6 masalah
    - Paragraf 3: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 11.0pt. Generator tidak mengisi konten paragraf ini.
    - Paragraf 5: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
    - Paragraf 6: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
    - Paragraf 7: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
    - Paragraf 8: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
    - Paragraf 9: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.

[TABLE] 1 masalah
    - TABLE MISSING: Output kekurangan 8 tabel (Expected: 9, Output: 1). Tabel layout container template (mis. figure side-by-side) kemungkinan hilang.

============================================================
  PROMPT SOLUSI PER KATEGORI (copy ke AI yang akan benerin AEJ_embedded_realgen.py)
============================================================

[SOLUSI UNTUK AEJ_embedded_realgen.py - STYLE PRESERVATION]
Skrip `AEJ_embedded_realgen.py` saat ini mereset custom style (seperti judul) menjadi Normal. Tolong modifikasi `AEJ_embedded_realgen.py` agar saat melakukan `paragraph.text = new_text`, skrip harus MEMPERTAHANKAN `paragraph.style` dan meng-copy properti font dari `run` sebelumnya.

[SOLUSI UNTUK AEJ_embedded_realgen.py - TABLE LAYOUT CONTAINER]
Jumlah tabel body output `AEJ_embedded_realgen.py` tidak match template original. Banyak template jurnal (mis. SAGE) menggunakan `<w:tbl>` sebagai container layout untuk figure/diagram side-by-side, kotak rules, atau highlight box -- bukan hanya tabel data. Tolong modifikasi `AEJ_embedded_realgen.py` supaya setiap blok figure/diagram fullwidth di-wrap dalam tabel 1-row container, dan blok rules/highlight juga pakai tabel mini, agar jumlah tabel body match template asli.

============================================================
  MASTER PROMPT PERBAIKAN (gabungan, siap copy-paste)
============================================================

Sistem QA mendeteksi masalah pada hasil generator `AEJ_embedded_realgen.py` untuk template `AEJ.docx`. Tolong perbaiki `AEJ_embedded_realgen.py` mengatasi semua masalah berikut sekaligus, tanpa mengubah logika lain yang sudah benar:

KATEGORI 3: STYLE & FONT
  - Paragraf 3: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 11.0pt. Generator tidak mengisi konten paragraf ini.
  - Paragraf 5: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
  - Paragraf 6: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
  - Paragraf 7: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
  - Paragraf 8: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.
  - Paragraf 9: paragraf kosong (tidak ada runs). Template memiliki konten dengan font 9.0pt. Generator tidak mengisi konten paragraf ini.

[SOLUSI UNTUK AEJ_embedded_realgen.py - STYLE PRESERVATION]
Skrip `AEJ_embedded_realgen.py` saat ini mereset custom style (seperti judul) menjadi Normal. Tolong modifikasi `AEJ_embedded_realgen.py` agar saat melakukan `paragraph.text = new_text`, skrip harus MEMPERTAHANKAN `paragraph.style` dan meng-copy properti font dari `run` sebelumnya.

KATEGORI 4: TABLE LAYOUT CONTAINER
  - TABLE MISSING: Output kekurangan 8 tabel (Expected: 9, Output: 1). Tabel layout container template (mis. figure side-by-side) kemungkinan hilang.

[SOLUSI UNTUK AEJ_embedded_realgen.py - TABLE LAYOUT CONTAINER]
Jumlah tabel body output `AEJ_embedded_realgen.py` tidak match template original. Banyak template jurnal (mis. SAGE) menggunakan `<w:tbl>` sebagai container layout untuk figure/diagram side-by-side, kotak rules, atau highlight box -- bukan hanya tabel data. Tolong modifikasi `AEJ_embedded_realgen.py` supaya setiap blok figure/diagram fullwidth di-wrap dalam tabel 1-row container, dan blok rules/highlight juga pakai tabel mini, agar jumlah tabel body match template asli.

============================================================

============================================================
  PROMPT UPGRADE auto_checker.py (META-IMPROVEMENT)
============================================================

Selain memperbaiki `AEJ_embedded_realgen.py`, gunakan temuan di atas sebagai masukan untuk meng-upgrade `auto_checker.py` supaya checker jauh lebih spesifik, valid, dan tahan terhadap pola anomali baru di template lain. Tolong analisis temuan ini dan perbarui checker sesuai panduan di bawah:

LANGKAH 1 -- Analisis temuan baru
  Untuk template `AEJ_embedded_real`, temuan berikut muncul:
    - KATEGORI 3: STYLE & FONT: 6 masalah
    - KATEGORI 4: TABLE LAYOUT CONTAINER: 1 masalah

  Pertanyaan kunci yang harus dijawab sebelum mengubah checker:
    a. Apakah pola anomali ini SUDAH ditangkap oleh audit existing,
       hanya saja threshold-nya kurang tepat? (=> tune threshold)
    b. Apakah pola ini BENAR-BENAR baru, belum ada audit-nya?
       (=> tambah audit function baru)
    c. Apakah finding ini false-positive untuk template-style ini?
       (=> tambah whitelist/exclusion, JANGAN hapus check global)

LANGKAH 2 -- Pilih strategi upgrade (urut dari paling aman)
  [A] Tune threshold existing
      Contoh: TABLE_COUNT_TOLERANCE, SECTPR_DISTRIBUTION_THRESHOLD,
      PARAGRAPH_STYLE_CHECK_COUNT. Ubah hanya bila pola yang
      terdeteksi konsisten muncul di >=2 template berbeda.

  [B] Tambah sub-check di audit existing
      Bila finding-nya varian dari kategori existing (mis. table
      count meleset karena pattern figure-wrapper), tambahkan
      cabang khusus di `audit_table_count()` atau sejenisnya
      sehingga pesan error lebih spesifik (sebut pattern-nya).

  [C] Tambah audit function baru
      Bila pola benar-benar di luar 7 kategori existing, buat
      `audit_<nama>()` baru yang return `AuditResult`, daftarkan
      di `main()`, dan tambahkan render-nya di `render_report()`.
      Konvensi: MAYOR untuk struktural, MINOR untuk visual.

LANGKAH 3 -- Validasi sebelum commit
  - Run checker terhadap SEMUA file output existing (DJLIT, SAGE,
    JCEF, dll) untuk pastikan tidak ada regresi false-positive.
  - Untuk template yang sebelumnya 'Lulus', skornya harus tetap
    >= skor sebelumnya. Bila ada penurunan, threshold/check baru
    terlalu agresif -- mundur atau perlonggar.
  - Tambahkan komentar di check baru yang menyebut template
    sumber pola: '# Pattern asal: template AEJ_embedded_real (run YYYY-MM-DD)'
    supaya jejak rule traceable di git history.

LANGKAH 4 -- Hindari over-fitting
  - JANGAN bikin rule yang hanya men-deteksi kasus persis di
    `AEJ_embedded_real` (mis. hard-code nilai twip atau nama style).
    Generalisasi ke pola struktural (rasio, range, signature XML).
  - JANGAN hapus/loosen check existing hanya supaya template ini
    'lulus'. Kalau check existing salah, perbaiki rumusnya, bukan
    matikan check-nya.
  - Determinisme dijaga: checker tetap pure-function (tidak baca
    state run sebelumnya, tidak ada randomness).

Output yang diharapkan: patch `auto_checker.py` + ringkasan singkat
alasan setiap perubahan, sehingga reviewer bisa verifikasi cepat.

============================================================